#include <stdio.h>
int main(){
/*int a[10];

    for(int x=0;x<10;x++){
        scanf("%d", &a[x]);
        if(a[x]<0){
        printf("%d", a[x]);
        }
    }
 return 0;*/
//--------------------------------------------------

/*int a;
int sum=0;
printf("Nechta son kiritasiz");
scanf("%d", &a);
int arr[a];

for(int x=0;x<a;x++){
    scanf("%d", &arr[x]);
    if(arr[x]<0){
        sum=sum+arr[x];
    }
}

    printf("%d", sum);*/
//-----------------------------------------------

/*int a;
int sum=0,sum2=0;
printf("Nechta son kiritasiz");
scanf("%d", &a);
int arr[a];

for(int x=0;x<a;x++){
    scanf("%d", &arr[x]);
    if(arr[x]%2==0){
        sum=sum+arr[x];
    }else(
        sum2=sum2-arr[x]
    );
}
 printf("%d-Juftlar yigindisi\n", sum);
 printf("%d-Toqlar ayirmasi", sum2);*/


}